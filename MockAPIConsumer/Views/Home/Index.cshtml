@using MockAPIConsumer.Models
@model IEnumerable<MockAPIConsumer.Models.Place>

@{
    ViewBag.Title = "Index";
}

<h2>Mock API Contents</h2>
@foreach (Place place in Model)
{
    @Html.Partial("_PlaceSummary", place)
}

@Html.ActionLink("Add", "Add", new { id = Model.Count() }, new { @class="btn btn-success pull-right"})